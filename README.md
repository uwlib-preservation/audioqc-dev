# audioqc-dev

To use must install: 

Requires:
 * Ruby Gems (optional)
   * Parallel (for parallel file processing)
 * CLI tools
   * FFmpeg/FFprobe
   * Mediainfo
   * Mediaconch
  
Configurations can be set in the associated CSV file
